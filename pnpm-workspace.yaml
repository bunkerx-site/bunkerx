packages:
  - 'apps/*'
  - 'packages/*'

# esbuild ships a platform binary through a postinstall script; Vite cannot
# start without it. pnpm 10 blocks install scripts unless they are named here.
onlyBuiltDependencies:
  - esbuild
